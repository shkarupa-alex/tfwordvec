{
  'input_unit':  'char',
        unit_case: 'same'
        label_case: 'same'
        unit_freq=0,
        label_freq=1,

        vect_model:'cbow'
        window_size=5,
        embed_type='dense',  # or 'adapt'
        embed_cutoff=[0],  # TODO
        embed_factor=4,  # TODO
        embed_size=256,
        samp_thold=1e-3,
        model_head='asm',  # or 'nce' or 'ss' or 'sm'
        head_cutoff=[0],  # TODO
        head_factor=4,  # TODO
        head_drop=0.,  # TODO
        neg_samples=5,

        batch_size=256,
        bucket_cbow=True,
        num_epochs=5,
        train_optim='adam',
        learn_rate=0.05
}